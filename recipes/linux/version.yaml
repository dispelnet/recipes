family:   linux
question: version
applies:  ["*"]

steps:
  - run: uname -sr
    template: |
      Value VERSION (\S+)

      Start
        ^Linux\s+${VERSION} -> Record
    fields:
      version: VERSION
