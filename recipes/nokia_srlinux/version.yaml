family:   nokia_srlinux
question: version
applies:  ["*"]

steps:
  - run: info from state /system information | as json
    template: |
      Value VERSION ([0-9][0-9.]*)

      Start
        ^\s+"version":\s+"v${VERSION} -> Record
    fields:
      version: VERSION
